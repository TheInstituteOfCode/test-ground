source "https://rubygems.org"

# When you change this file run `bundle install`. Run Jekyll with `bundle exec`, like so:
#
#     bundle exec jekyll serve
#     bundle exec jekyll server --watch --livereload
#

# Plugins
gem "jekyll", '4.2.2'
gem "webrick"
gem "jekyll-paginate-v2"
gem 'jekyll-sitemap'
